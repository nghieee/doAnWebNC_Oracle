@model web_ban_thuoc.Models.ProfileViewModel
@using web_ban_thuoc.Models
@{
    ViewData["Title"] = "Thông tin cá nhân";
    Layout = "_Layout";
    ViewData["CurrentPage"] = "ProfileDetail";
}
@Html.AntiForgeryToken()
@if (TempData["ShowToast"] != null && TempData["ProfileSuccess"] != null)
{
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            showToast('@Html.Raw(TempData["ProfileSuccess"]?.ToString())', 'success');
        });
    </script>
}
@if (TempData["ShowToast"] != null && TempData["ProfileError"] != null)
{
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            showToast('@Html.Raw(TempData["ProfileError"]?.ToString())', 'error');
        });
    </script>
}
<div>
    <div class="container pt-3">
        <div class="row">
            <div class="col-12 col-lg-4 mb-lg-0">
                <div class="sticky-sidebar mb-5">
                    <div class="bg-white shadow-sm rounded-4 p-0">
                        <div class="text-center pt-4 pb-3 border-bottom">
                            <img src="https://nhathuoclongchau.com.vn/estore-images/profile/v2/avatar-profile-large.svg" style="width: 96px; height: 96px;" class="mb-2 rounded-circle bg-light border" alt="avatar">
                            <div class="fw-bold fs-5">@Model.UserName</div>
                            <div class="text-muted small">@Model.Email</div>
                        </div>
                        <ul class="nav flex-lg-column flex-row nav-pills gap-2 p-3" id="profileTab" role="tablist">
                            <li class="nav-item" role="presentation">
                                <button class="nav-link active w-100 text-start" id="info-tab" data-bs-toggle="pill" data-bs-target="#info" type="button" role="tab" aria-controls="info" aria-selected="true">
                                    <i class="fas fa-user me-2"></i>Thông tin cá nhân
                                </button>
                            </li>
                            <li class="nav-item" role="presentation">
                                <button class="nav-link w-100 text-start" id="orders-tab" data-bs-toggle="pill" data-bs-target="#orders" type="button" role="tab" aria-controls="orders" aria-selected="false">
                                    <i class="fas fa-box me-2"></i>Đơn hàng
                                </button>
                            </li>
                            <li class="nav-item" role="presentation">
                                <button class="nav-link w-100 text-start" id="gift-tab" data-bs-toggle="pill" data-bs-target="#gift" type="button" role="tab" aria-controls="gift" aria-selected="false">
                                    <i class="fas fa-gift me-2"></i>Quà tặng
                                </button>
                            </li>
                        </ul>
                    </div>
                </div>
            </div>
            <div class="col-12 col-lg-8">
                <div class="tab-content" id="profileTabContent">
                    <div class="tab-pane fade show active" id="info" role="tabpanel" aria-labelledby="info-tab">
                        <div class="bg-white mb-5 shadow-sm rounded-4">
                            <div class="pt-3 px-4 border-bottom">
                                <p class="fw-bold fs-5">Thông tin cá nhân</p>
                            </div>
                            <div class="pt-3 me-auto ms-auto" style="max-width: 440px;">
                                <div class="text-center pt-4 pb-4">
                                    <div class="px-4">
                                        <div class="d-flex justify-content-between pt-4 pb-4 border-bottom">
                                            <span class="text-muted">Tên người dùng</span>
                                            <span class="fw-semibold">@Model.UserName</span>
                                        </div>
                                        <div class="d-flex justify-content-between pt-4 pb-4 border-bottom">
                                            <span class="text-muted">Email</span>
                                            <span class="fw-semibold">@Model.Email</span>
                                        </div>
                                        <div class="d-flex justify-content-between pt-4 pb-4 border-bottom">
                                            <span class="text-muted">Số điện thoại</span>
                                            <span class="fw-semibold">@Model.PhoneNumber</span>
                                        </div>
                                    </div>
                                    <button type="button" class="btn btn-outline-primary mb-5 mt-5 px-4 py-2 rounded-pill fw-semibold" data-bs-toggle="modal" data-bs-target="#editProfileModal">
                                        <i class="fas fa-edit me-2"></i>Chỉnh sửa thông tin
                                    </button>
                                </div>
                            </div>
                        </div>
                        <!-- Modal chỉnh sửa thông tin -->
                        <div class="modal fade" id="editProfileModal" tabindex="-1" aria-labelledby="editProfileModalLabel" aria-hidden="true">
                          <div class="modal-dialog modal-dialog-centered">
                            <div class="modal-content">
                              <div class="modal-header">
                                <h5 class="modal-title" id="editProfileModalLabel">Chỉnh sửa thông tin cá nhân</h5>
                                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Đóng"></button>
                              </div>
                              <div class="modal-body">
                                @if (Model.RequireVerification)
                                {
                                    <form asp-action="VerifyProfile" method="post" id="verifyForm">
                                        <input type="hidden" name="VerificationType" value="@Model.VerificationType" />
                                        <div class="mb-3 text-center">
                                            <label class="form-label mb-3">Nhập mã xác nhận gồm 6 số đã gửi qua email của bạn</label>
                                            <div class="d-flex justify-content-center gap-2 mb-2">
                                                @for (int i = 0; i < 6; i++)
                                                {
                                                    <input type="text" maxlength="1" pattern="[0-9]" inputmode="numeric" class="form-control text-center code-input" name="CodeDigit" style="width: 40px; font-size: 1.5rem;" required />
                                                }
                                            </div>
                                            <input type="hidden" name="VerificationCode" id="VerificationCode" />
                                            @Html.ValidationMessage("VerificationCode", null, new { @class = "text-danger" })
                                        </div>
                                        <button type="submit" class="btn btn-primary w-100">Xác nhận</button>
                                    </form>
                                }
                                else
                                {
                                    <form asp-action="Profile" method="post" id="profileForm">
                                        <div class="mb-3">
                                            <label class="form-label">Tên hiển thị</label>
                                            <input type="text" name="UserName" class="form-control" value="@Model.UserName" required />
                                        </div>
                                        <div class="mb-3">
                                            <label class="form-label">Email</label>
                                            <input type="email" class="form-control" value="@Model.Email" disabled />
                                        </div>
                                        <div class="mb-3">
                                            <label class="form-label">Số điện thoại</label>
                                            <input type="text" name="PhoneNumber" class="form-control" value="@Model.PhoneNumber" />
                                        </div>
                                        <hr />
                                        <div class="mb-3">
                                            <label class="form-label">Đổi email mới</label>
                                            <input type="email" name="NewEmail" class="form-control" placeholder="Nhập email mới nếu muốn đổi" />
                                        </div>
                                        <div class="mb-3">
                                            <label class="form-label">Mật khẩu cũ</label>
                                            <input type="password" name="CurrentPassword" class="form-control" placeholder="Nhập mật khẩu hiện tại nếu muốn đổi mật khẩu" />
                                        </div>
                                        <div class="mb-3">
                                            <label class="form-label">Đổi mật khẩu mới</label>
                                            <input type="password" name="NewPassword" class="form-control" placeholder="Nhập mật khẩu mới nếu muốn đổi" />
                                        </div>
                                        <div class="mb-3">
                                            <label class="form-label">Xác nhận mật khẩu mới</label>
                                            <input type="password" name="ConfirmPassword" class="form-control" placeholder="Nhập lại mật khẩu mới" />
                                            @Html.ValidationMessage("ConfirmPassword", null, new { @class = "text-danger" })
                                        </div>
                                        <button type="submit" class="btn btn-primary w-100">Cập nhật thông tin</button>
                                    </form>
                                }
                              </div>
                            </div>
                          </div>
                        </div>
                        <script>
                        // Tự động mở lại modal xác thực nếu RequireVerification
                        document.addEventListener('DOMContentLoaded', function() {
                            if (@(Model.RequireVerification.ToString().ToLower())) {
                                var modal = new bootstrap.Modal(document.getElementById('editProfileModal'));
                                setTimeout(function() { modal.show(); }, 300);
                            }
                        });
                        // Xử lý nhập 6 ô mã xác nhận
                        document.addEventListener('DOMContentLoaded', function() {
                            var codeInputs = document.querySelectorAll('.code-input');
                            if (codeInputs.length > 0) {
                                codeInputs[0].focus();
                                codeInputs.forEach((input, idx) => {
                                    input.addEventListener('input', function(e) {
                                        if (this.value.length === 1 && idx < 5) {
                                            codeInputs[idx+1].focus();
                                        }
                                        updateHiddenCode();
                                    });
                                    input.addEventListener('keydown', function(e) {
                                        if (e.key === 'Backspace' && this.value === '' && idx > 0) {
                                            codeInputs[idx-1].focus();
                                        }
                                    });
                                });
                                function updateHiddenCode() {
                                    var code = '';
                                    codeInputs.forEach(i => code += i.value);
                                    document.getElementById('VerificationCode').value = code;
                                }
                            }
                        });
                        </script>
                    </div>
                    <div class="tab-pane fade" id="orders" role="tabpanel" aria-labelledby="orders-tab">
                        <div class="bg-white shadow-sm rounded-4 p-4 mb-5">
                            <h5 class="fw-bold mb-4"><i class="fas fa-box me-2"></i>Lịch sử đơn hàng</h5>
                            
                            @if (!Model.Orders.Any())
                            {
                                <div class="text-center py-5">
                                    <i class="fas fa-box-open text-muted mb-3" style="font-size: 4rem;"></i>
                                    <h6 class="text-muted mb-2">Bạn chưa có đơn hàng nào</h6>
                                    <p class="text-muted small">Hãy mua sắm để có đơn hàng đầu tiên!</p>
                                    <a href="/" class="btn btn-primary rounded-pill px-4 py-2">
                                        <i class="fas fa-shopping-bag me-2"></i>Mua sắm ngay
                                    </a>
                                </div>
                            }
                            else
                            {
                                <div class="accordion" id="ordersAccordion">
                                    @foreach (var order in Model.Orders)
                                    {
                                        <div class="accordion-item border-0 mb-3 shadow-sm rounded-3">
                                            <h2 class="accordion-header" id="heading@(order.OrderId)">
                                                <button class="accordion-button collapsed bg-white rounded-3" type="button" data-bs-toggle="collapse" data-bs-target="#collapse@(order.OrderId)" aria-expanded="false" aria-controls="collapse@(order.OrderId)">
                                                    <div class="d-flex justify-content-between align-items-center w-100 me-3">
                                                        <div class="d-flex align-items-center">
                                                            <div class="me-3">
                                                                <div class="fw-bold text-dark">Đơn hàng #@order.OrderId</div>
                                                                <div class="small text-muted">@order.OrderDate.ToString("dd/MM/yyyy HH:mm")</div>
                                                            </div>
                                                        </div>
                                                        <div class="text-end">
                                                            <div class="fw-bold text-primary">@order.TotalAmount.ToString("N0")đ</div>
                                                            <div class="small">
                                                                <span class="badge @GetStatusBadgeClass(order.Status)">@GetStatusText(order.Status)</span>
                                                            </div>
                                                        </div>
                                                    </div>
                                                </button>
                                            </h2>
                                            <div id="collapse@(order.OrderId)" class="accordion-collapse collapse" aria-labelledby="heading@(order.OrderId)" data-bs-parent="#ordersAccordion">
                                                <div class="accordion-body pt-0">
                                                    <!-- Thông tin đơn hàng -->
                                                    <div class="row mb-3">
                                                        <div class="col-md-6">
                                                            <div class="small text-muted mb-1">Người nhận:</div>
                                                            <div class="fw-semibold">@order.FullName</div>
                                                            <div class="small text-muted">@order.Phone</div>
                                                        </div>
                                                        <div class="col-md-6">
                                                            <div class="small text-muted mb-1">Địa chỉ:</div>
                                                            <div class="small">@order.ShippingAddress</div>
                                                        </div>
                                                    </div>
                                                    
                                                    <!-- Danh sách sản phẩm -->
                                                    <div class="border-top pt-3">
                                                        <h6 class="fw-semibold mb-3">Sản phẩm đã đặt:</h6>
                                                        @foreach (var item in order.Items)
                                                        {
                                                            <div class="d-flex align-items-center mb-3 p-3 bg-light rounded-3">
                                                                <img src="/images/products/@item.ImageUrl" alt="@item.ProductName" class="rounded me-3" style="width: 60px; height: 60px; object-fit: cover;">
                                                                <div class="flex-grow-1">
                                                                    <div class="fw-semibold">@item.ProductName</div>
                                                                    <div class="small text-muted">Số lượng: @item.Quantity</div>
                                                                </div>
                                                                <div class="text-end">
                                                                    <div class="fw-semibold">@item.Price.ToString("N0")đ</div>
                                                                    <div class="small text-muted">@item.TotalPrice.ToString("N0")đ</div>
                                                                </div>
                                                            </div>
                                                        }
                                                    </div>
                                                    
                                                    <!-- Tổng tiền -->
                                                    <div class="border-top pt-3">
                                                        <div class="d-flex justify-content-between align-items-center">
                                                            <span class="fw-bold">Tổng cộng:</span>
                                                            <span class="fw-bold text-primary fs-5">@order.TotalAmount.ToString("N0")đ</span>
                                                        </div>
                                                        <div class="d-flex justify-content-between align-items-center mt-2">
                                                            <span class="small text-muted">Hình thức thanh toán:</span>
                                                            <span class="badge @(order.PaymentMethod == "PayOS" ? "bg-primary" : "bg-secondary")">
                                                                @(order.PaymentMethod == "PayOS" ? "Thanh toán online" : "Thanh toán khi nhận hàng")
                                                            </span>
                                                        </div>
                                                        <div class="d-flex justify-content-between align-items-center mt-2">
                                                            <span class="small text-muted">Trạng thái thanh toán:</span>
                                                            <span class="badge @GetPaymentStatusBadgeClass(order.PaymentStatus)">@GetPaymentStatusText(order.PaymentStatus)</span>
                                                        </div>
                                                        @if (!string.IsNullOrEmpty(order.TrackingCode))
                                                        {
                                                            <div class="mt-3 p-2 rounded-3 bg-light small">
                                                                <i class="fas fa-truck text-primary me-1"></i>
                                                                <strong>@order.Carrier</strong> — Mã vận đơn: <code>@order.TrackingCode</code>
                                                                @if (!string.IsNullOrEmpty(order.TrackingUrl))
                                                                {
                                                                    <div class="mt-1"><a href="@order.TrackingUrl" target="_blank" rel="noopener">Theo dõi đơn hàng →</a></div>
                                                                }
                                                            </div>
                                                        }
                                                    </div>
                                                    
                                                    <!-- Nút thanh toán cho đơn hàng online chưa thanh toán -->
                                                    @if (order.PaymentMethod == "PayOS" && order.PaymentStatus == PaymentStatuses.Unpaid)
                                                    {
                                                        <div class="border-top pt-3">
                                                            <button class="btn btn-primary btn-pay-online" data-order-id="@order.OrderId">
                                                                <i class="fas fa-credit-card me-2"></i>Thanh toán ngay
                                                            </button>
                                                        </div>
                                                    }
                                                    
                                                    @if (OrderStatuses.CanCustomerCancel(order.Status ?? ""))
                                                    {
                                                        <button class="btn btn-danger btn-cancel-order mt-3" data-id="@order.OrderId">Hủy đơn</button>
                                                    }
                                                </div>
                                            </div>
                                        </div>
                                    }
                                </div>
                            }
                        </div>
                    </div>
                    <div class="tab-pane fade" id="gift" role="tabpanel" aria-labelledby="gift-tab">
                        <div class="bg-white shadow-sm rounded-4 p-4 mb-5">
                            <h5 class="fw-bold mb-4"><i class="fas fa-gift me-2"></i>Quà tặng & Ưu đãi</h5>
                            <div class="mb-3">
                                <span class="fw-semibold">Tổng tiền đã tiêu: </span>
                                <span class="text-primary fw-bold">@Model.TotalSpent.ToString("N0")đ</span>
                            </div>
                            <div class="mb-3">
                                <span class="fw-semibold">Tổng tiền đã tiêu 6 tháng gần nhất: </span>
                                <span class="text-primary fw-bold">@Model.TotalSpent6Months.ToString("N0")đ</span>
                            </div>
                            <div class="mb-3">
                                <span class="fw-semibold">Hạng thành viên: </span>
                                <span class="badge bg-info text-white">@Model.Rank</span>
                            </div>
                            <div class="mb-4 p-3 rounded-3" style="background: linear-gradient(135deg, #fffbeb, #fef3c7);">
                                <div class="d-flex flex-wrap align-items-center justify-content-between gap-2">
                                    <div>
                                        <span class="fw-semibold">Điểm thưởng: </span>
                                        <span class="badge bg-warning text-dark fs-6" id="loyalty-points-badge">@Model.LoyaltyPoints.ToString("N0") điểm</span>
                                    </div>
                                    <span class="small text-muted">Tích điểm khi đơn được giao · Đổi quà bên dưới</span>
                                </div>
                            </div>

                            <h6 class="fw-bold mb-3"><i class="fas fa-store me-2 text-primary"></i>Cửa hàng đổi quà</h6>
                            <div class="row g-3 mb-4">
                                @foreach (var reward in Model.LoyaltyRewards)
                                {
                                    <div class="col-md-6 col-lg-4">
                                        <div class="card h-100 border-0 shadow-sm rounded-3 @(reward.CanRedeem ? "" : "opacity-75")">
                                            <div class="card-body d-flex flex-column">
                                                <div class="d-flex justify-content-between align-items-start mb-2">
                                                    <h6 class="fw-bold mb-0">@reward.Title</h6>
                                                    <span class="badge bg-warning text-dark">@reward.PointsCost.ToString("N0") điểm</span>
                                                </div>
                                                @if (!string.IsNullOrEmpty(reward.Description))
                                                {
                                                    <p class="small text-muted mb-2">@reward.Description</p>
                                                }
                                                <div class="small mb-2">
                                                    <span class="badge bg-primary-subtle text-primary">@reward.RewardLabel</span>
                                                    @if (!string.IsNullOrEmpty(reward.RequiredRank))
                                                    {
                                                        <span class="badge bg-info-subtle text-info ms-1">@reward.RequiredRank+</span>
                                                    }
                                                    @if (reward.StockRemaining.HasValue)
                                                    {
                                                        <span class="badge bg-secondary-subtle text-secondary ms-1">Còn @reward.StockRemaining</span>
                                                    }
                                                </div>
                                                <div class="mt-auto">
                                                    @if (reward.CanRedeem)
                                                    {
                                                        <button type="button" class="btn btn-primary btn-sm w-100 btn-redeem-reward" data-id="@reward.LoyaltyRewardId" data-title="@reward.Title" data-points="@reward.PointsCost">
                                                            <i class="fas fa-gift me-1"></i> Đổi ngay
                                                        </button>
                                                    }
                                                    else
                                                    {
                                                        <button type="button" class="btn btn-outline-secondary btn-sm w-100" disabled title="@reward.DisabledReason">@reward.DisabledReason</button>
                                                    }
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                }
                                @if (!Model.LoyaltyRewards.Any())
                                {
                                    <div class="col-12 text-muted small">Chưa có quà đổi điểm. Vui lòng quay lại sau.</div>
                                }
                            </div>

                            @if (Model.RedeemHistory.Any())
                            {
                                <h6 class="fw-bold mb-2"><i class="fas fa-clock-rotate-left me-2"></i>Lịch sử đổi quà</h6>
                                <ul class="list-group list-group-flush mb-4 small">
                                    @foreach (var h in Model.RedeemHistory)
                                    {
                                        <li class="list-group-item px-0 d-flex justify-content-between">
                                            <span>@h.Title</span>
                                            <span class="text-danger">-@h.Points.ToString("N0") điểm · @h.CreatedAt.ToString("dd/MM/yyyy")</span>
                                        </li>
                                    }
                                </ul>
                            }

                            <div class="mb-3">
                                <span class="fw-semibold">Danh sách voucher:</span>
                                <table class="table table-bordered mt-2">
                                    <thead>
                                        <tr>
                                            <th>Mã voucher</th>
                                            <th>Mô tả</th>
                                            <th>Loại</th>
                                            <th>Phần trăm giảm</th>
                                            <th>Điều kiện áp dụng</th>
                                            <th>Hạn sử dụng</th>
                                            <th>Trạng thái</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @foreach (var v in Model.Vouchers)
                                        {
                                            <tr>
                                                <td>@v.Code</td>
                                                <td>@v.Description</td>
                                                <td>
                                                    @if (v.DiscountType == "Percent")
                                                    {
                                                        <span class="badge bg-primary">Giảm % tổng đơn</span>
                                                    }
                                                    else if (v.DiscountType == "Category")
                                                    {
                                                        <span class="badge bg-info">Giảm theo danh mục</span>
                                                    }
                                                </td>
                                                <td>
                                                    @if (v.PercentValue.HasValue)
                                                    {
                                                        <span class="fw-bold">@v.PercentValue.Value.ToString("N0")%</span>
                                                    }
                                                    else if (v.DiscountAmount.HasValue)
                                                    {
                                                        <span class="fw-bold">@v.DiscountAmount.Value.ToString("N0")đ</span>
                                                    }
                                                </td>
                                                <td>
                                                    @if (!string.IsNullOrEmpty(v.Detail))
                                                    {
                                                        <span>@v.Detail</span>
                                                    }
                                                    else if (v.DiscountType == "FullOrder")
                                                    {
                                                        <span>Áp dụng cho toàn bộ đơn hàng</span>
                                                    }
                                                    else if (v.DiscountType == "Category" && !string.IsNullOrEmpty(v.CategoryName))
                                                    {
                                                        <span>Chỉ áp dụng cho sản phẩm thuộc danh mục <b>@v.CategoryName</b></span>
                                                    }
                                                </td>
                                                <td>@v.ExpiryDate.ToString("dd/MM/yyyy")</td>
                                                <td>
                                                    @if (v.IsUsed)
                                                    {
                                                        <span class="badge bg-secondary">Đã sử dụng</span>
                                                    }
                                                    else if (v.IsExpired)
                                                    {
                                                        <span class="badge bg-danger">Hết hạn</span>
                                                    }
                                                    else
                                                    {
                                                        <span class="badge bg-success">Khả dụng</span>
                                                    }
                                                </td>
                                            </tr>
                                        }
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
<style>
    .breadcrumb {
        background: #fff;
    }
    .list-group-item.active, .nav-link.active {
        background: #1250dc;
        border-color: #1250dc;
        color: #fff !important;
    }
    .nav-link {
        color: #333;
    }
    .code-input {
        border: 1px solid #ccc;
        border-radius: 8px;
        font-weight: bold;
        font-size: 1.5rem;
        width: 40px;
        height: 48px;
        text-align: center;
        outline: none;
        transition: border-color 0.2s;
    }
    .code-input:focus {
        border-color: #1250dc;
        box-shadow: 0 0 0 2px #eaeffa;
    }
    
    /* Sticky Sidebar */
    .sticky-sidebar {
        position: sticky;
        top: 20px;
        z-index: 100;
    }
    
    /* Responsive cho mobile */
    @@media (max-width: 991.98px) {
        .sticky-sidebar {
            position: static;
            top: auto;
        }
    }
</style>

@functions {
    string GetStatusText(string status) => status ?? "";
    
    string GetStatusBadgeClass(string status) => status switch
    {
        OrderStatuses.PendingPayment => "bg-secondary text-white",
        OrderStatuses.PendingConfirmation => "bg-warning text-dark",
        OrderStatuses.Confirmed => "bg-info text-white",
        OrderStatuses.Packing => "bg-primary text-white",
        OrderStatuses.Shipped => "bg-primary text-white",
        OrderStatuses.Delivered => "bg-success text-white",
        OrderStatuses.Cancelled => "bg-danger text-white",
        _ => "bg-secondary text-white"
    };
    
    string GetPaymentStatusText(string paymentStatus)
    {
        return paymentStatus switch
        {
            "Chưa thanh toán" => "Chưa thanh toán",
            "Đã thanh toán" => "Đã thanh toán",
            "Pending" => "Chờ xử lý",
            "Completed" => "Hoàn thành",
            "Failed" => "Thất bại",
            _ => paymentStatus
        };
    }
    
    string GetPaymentStatusBadgeClass(string paymentStatus)
    {
        return paymentStatus switch
        {
            "Chưa thanh toán" => "bg-warning text-dark",
            "Đã thanh toán" => "bg-success text-white",
            "Pending" => "bg-info text-white",
            "Completed" => "bg-success text-white",
            "Failed" => "bg-danger text-white",
            _ => "bg-secondary text-white"
        };
    }
} 
@section Scripts {
<script>
$(function() {
    $('.btn-cancel-order').on('click', function() {
        var orderId = $(this).data('id');
        showConfirm('Bạn có chắc chắn muốn hủy đơn hàng này?', { icon: 'warning', confirmButtonText: 'Hủy đơn', confirmButtonColor: '#e03131' }).then(function(ok) {
            if (!ok) return;
            $.post('/Cart/CancelOrder', { orderId: orderId }, function(res) {
                if (res.success) {
                    showAlert('Đã hủy đơn hàng!', { icon: 'success' }).then(function() { location.reload(); });
                } else {
                    showAlert(res.message || 'Hủy đơn thất bại!', { icon: 'error' });
                }
            });
        });
    });
    
    // Xử lý nút thanh toán online
    $('.btn-pay-online').on('click', function() {
        var orderId = $(this).data('order-id');
        var button = $(this);
        
        // Disable button để tránh click nhiều lần
        button.prop('disabled', true).html('<i class="fas fa-spinner fa-spin me-2"></i>Đang xử lý...');
        
        // Chuyển hướng đến trang thanh toán PayOS
        window.location.href = '/PayOS/CreatePayment?orderId=' + orderId;
    });

    $(document).on('click', '.btn-redeem-reward', function() {
        var btn = $(this);
        var id = btn.data('id');
        var title = btn.data('title');
        var points = btn.data('points');
        showConfirm('Đổi "' + title + '" với ' + points + ' điểm?', { icon: 'question', confirmButtonText: 'Đổi ngay' }).then(function(ok) {
            if (!ok) return;
            btn.prop('disabled', true).html('<i class="fas fa-spinner fa-spin"></i>');
            $.ajax({
                url: '/api/loyalty/redeem',
                type: 'POST',
                contentType: 'application/json',
                data: JSON.stringify({ rewardId: id }),
                success: function(res) {
                    if (res.success) {
                        showAlert(res.message, { icon: 'success' }).then(function() { location.reload(); });
                    } else {
                        showAlert(res.message || 'Đổi quà thất bại', { icon: 'error' });
                        btn.prop('disabled', false).html('<i class="fas fa-gift me-1"></i> Đổi ngay');
                    }
                },
                error: function() {
                    showAlert('Có lỗi xảy ra. Vui lòng thử lại.', { icon: 'error' });
                    btn.prop('disabled', false).html('<i class="fas fa-gift me-1"></i> Đổi ngay');
                }
            });
        });
    });
});
</script>
} 